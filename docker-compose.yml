version: '3.8'

services:
  trimatrix:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always
    # Add other services like a database if needed
    # db:
    #   image: postgres:15-alpine
    #   environment:
    #     POSTGRES_DB: trimatrix
    #     POSTGRES_USER: user
    #     POSTGRES_PASSWORD: password
    #   volumes:
    #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
